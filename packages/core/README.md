# @motionpath/core

Core package boundary for schema validation, runtime composition, plugins, Engine lifecycle, and platform adapters. The implementation is being moved here from `src/` in the v4.1 extraction; do not add new runtime files to the legacy root.
