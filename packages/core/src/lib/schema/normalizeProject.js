import { resolveTrack } from "../../usecases/ResolveTrack.js";
function freeze(value) { if (!value || typeof value !== "object" || Object.isFrozen(value)) return value; for (const child of Object.values(value)) freeze(child); return Object.freeze(value); }
function normalizeTrack(track, templates) { const resolved = resolveTrack(track, templates); if (!resolved) throw new Error(`normalizeProject: invalid track "${track?.id || "unknown"}".`); return freeze({ ...resolved, __normalized: true, keyframes: { ...(resolved.keyframes || {}) } }); }
export function normalizeProject(schema = {}) { const templates = (schema.templates || []).map((template) => freeze({ ...template, keyframes: { ...(template.keyframes || {}) } })); const normalizedTemplates = freeze(templates); const motionConfigs = (schema.motions || []).map((motion) => freeze({ ...motion, tracks: (motion.tracks || []).map((track) => normalizeTrack(track, normalizedTemplates)) })); const standaloneTracks = (schema.tracks || []).map((track) => normalizeTrack(track, normalizedTemplates)); return freeze({ ...schema, templates: normalizedTemplates, motions: motionConfigs, tracks: freeze(standaloneTracks) }); }
