# MotionPath v4.3 Graph Correctness: Session Handoff

**Date:** 2026-08-03  
**Base branch:** `v4.3`  
**Merged PRs:** [#65](https://github.com/chahyasantoso/motionpath/pull/65), [#68](https://github.com/chahyasantoso/motionpath/pull/68), [#70](https://github.com/chahyasantoso/motionpath/pull/70), [#71](https://github.com/chahyasantoso/motionpath/pull/71), [#72](https://github.com/chahyasantoso/motionpath/pull/72)

## Current state

v4.3 graph correctness is complete and merged. All five CI checks are green on the final retry-budget implementation.

- **#65:** graph IR, lifecycle-safe Track edges, reverse observer cleanup, atomic rewiring, GraphBinding, GraphPublisher scheduling/cache, Motion/TrackGroup integration.
- **#68:** publish-retry state separation, failure-mode regression coverage, and D3 contract reconciliation.
- **#70:** 500-seed fuzz gates and stale order-only doc cleanup.
- **#71:** narrow cache invalidation across GraphBinding commits, corrected partial-publish fuzz expectations, and CI trigger fixes.
- **#72:** opt-in bounded publish retries with `maxAttempts`, flush-based `backoff`, `onExhausted`, and `resetRetry(id)`.

## Settled failure semantics

- **Compose failure:** no patch exists; block the node's downstream closure for the flush, keep it state-pending, and retry source plus dependents next flush. Compose failures never consume publish retry budget.
- **Publish failure:** patch is valid; dependents publish in the same flush, only the failed node retries, and the retry does not enter `changed`.
- **Retry exhaustion:** default `maxAttempts: Infinity`, `backoff: 0` preserves old behavior. Opt-in budgets stop retrying after N failed publish calls, skip configured complete flushes between attempts, retain the valid cache by default, and allow explicit `resetRetry(id)` recovery. Failures still surface as `AggregateError`.

## Coverage map

- `GraphPublisher.contract.test.js`: dependent publishing, strict IDs, publish-failure isolation/retry scope, mutable topology, ID uniqueness, single compose context, locked behavior.
- `GraphPublisher.incremental.test.js`: idle cache, downstream invalidation, compose-failure blocking, independent branches, bounded attempts, backoff, reset, exhaustion, and compose-failure budget isolation.
- `GraphPublisher.fuzz.test.js`: 500 seeds per property, including cross-flush cache staleness and GraphBinding mutation agreement.
- `GraphBinding.test.js`: representation agreement, atomic mutation, rollback, destroy lifecycle.

## Next jobs

1. **Optional retry telemetry:** expose exhausted-node visibility only if a renderer has a concrete consumer. No automatic policy selection.
2. **Retry status ergonomics:** consider a read-only status accessor or callback if callers need to show recovery state.
3. **No live observation migration:** the source search found no app/package bypasses outside Track tests and fixtures.
4. **No graphify update:** generated graph artifacts are intentionally out of scope.

Do not invent more runtime behavior until a consumer requires it. v4.3 is in a stable state.

## Workflow rules

- Follow-up PRs target `v4.3`, not `master`.
- If CI is red, preserve the unit-test failure log before diagnosing.
- Keep standalone mutual observation and per-call Track compose semantics unchanged.
- Do not add a Track cross-frame cache. Cross-flush caching belongs to GraphPublisher.
- Use real Track fixtures for graph tests, never fake object-literal tracks.

## Checklist

- [x] Diagnose publish-retry failure.
- [x] Separate retry state from invalidation state.
- [x] Add compose-failure and independent-branch regression tests.
- [x] Reconcile D3 contract expectation.
- [x] 500-seed DAG fuzzing and stale-doc cleanup.
- [x] Preserve unaffected cache across graph commits.
- [x] Fix CI filters for `v4.3` and `fix/**`.
- [x] Add bounded publish retry policy and tests.
- [x] Document retry failure semantics.
- [x] Merge PR #72.
- [x] Record graphify as intentionally skipped.
